class Solution:
    def maxDepth(self, s: str) -> int:
        stack = []
        count = 0
        maxCount = 0
        
        for c in s:
            if c == '(':
                count += 1
                maxCount = max(count, maxCount)
            if c == ')':
                count -= 1
        return maxCount
